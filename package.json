{
  "name": "vibespec",
  "version": "0.1.0",
  "private": true,
  "description": "VibeSpec \u2014 a spec-first standard for agentic software.",
  "homepage": "https://vibespec.vibecodeunited.com",
  "scripts": {
    "validate": "node tools/validate.js",
    "scaffold:carcrm": "node tools/scaffold-from-json.js",
    "docs:generate": "node tools/generate-docs.js",
    "site:dev": "cd website && npx docusaurus start",
    "site:install": "npm ci --prefix website",
    "site:build": "npm run docs:generate && npm run site:install && cd website && npx docusaurus build --out-dir ../docs"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  }
}